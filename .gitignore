node_modules/
dist/
.data/

# Environment / secrets
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Local design / exploration notes (not part of the published repo)
design/

# ai-code-review-factory output when run locally in a checkout/worktree
.ai-review/

# vitest cache (relocated out of node_modules via cacheDir in vitest.config.ts)
.vite/
